actionTypes = {
  PLAY_CARD: "PLAY_CARD",
  RAISE_BET: "RAISE_BET",
  CALL: "CALL"
};

initialState = {};

function nextState(state, action) {
  switch (action.type) {
    case PLAY_CARD:
  }
  return state;
}

module.exports = { initialState, nextState };
